name = "arsd-resolver"
main = "worker.js"
compatibility_date = "2024-11-01"

# ============================================================
# كاش اختياري عبر KV (مُوصى به لتقليل الطلبات لموقع المصدر)
# خطوات التفعيل:
#   1) npx wrangler kv namespace create LINK_CACHE
#   2) انسخ الـ id الناتج وضعه أدناه بدل ضع-id-الـ-KV-هنا
#   3) أزل علامة # عن الأسطر الثلاثة التالية ثم أعد النشر
# ------------------------------------------------------------
# [[kv_namespaces]]
# binding = "LINK_CACHE"
# id = "ضع-id-الـ-KV-هنا"
